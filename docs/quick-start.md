## Quick start

This page is a thin redirect to the [README quick-start](https://github.com/zakuro-ai/zakuro#quick-start) until the hosted docs are wired to render the README directly. Both walk you through:

1. `pip install zakuro-ai`
2. A three-line remote-function example
3. The Adam-style adaptive allocator across a pool of workers

The README is the canonical source while the docs site bootstraps.
